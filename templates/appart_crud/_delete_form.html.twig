<form method="post" action="{{ path('app_appart_crud_delete', {'id': appart.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appart.id) }}">
    <button class="btn">Delete</button>
</form>
